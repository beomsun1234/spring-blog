spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/bs
    username: sa
    password: 1234
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  profiles:
    include: oauth